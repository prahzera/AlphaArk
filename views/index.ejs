<!-- Hero Section -->
<section class="hero">
  <div class="hero-background">
    <div class="hero-overlay"></div>
    <div class="hero-particles"></div>
  </div>
  <div class="hero-content">
    <div class="hero-text">
      <h1 class="hero-title">
        <span class="hero-title-main">WELCOME TO</span>
        <span class="hero-title-brand">ALPHA ARK</span>
      </h1>
      <p class="hero-description">Discover the best ARK: Survival Evolved experience on our optimized servers. Connect easily and enjoy exclusive mods, special events and an active community.</p>
      <div class="hero-buttons">
        <a href="/ips" class="btn btn-primary btn-hero">
          <i class="fas fa-server"></i>
          <span>View Servers</span>
        </a>
        <a href="/store" class="btn btn-secondary btn-hero">
          <i class="fas fa-shopping-cart"></i>
          <span>Store</span>
        </a>
      </div>
    </div>
    <div class="hero-visual">
      <div class="hero-stats">
        <div class="hero-stat">
          <span class="hero-stat-number" id="heroActivePlayers">0</span>
          <span class="hero-stat-label">Online Players</span>
        </div>
        <div class="hero-stat">
          <span class="hero-stat-number" id="heroTotalServers">0</span>
          <span class="hero-stat-label">Active Servers</span>
        </div>
      </div>
    </div>
  </div>
  <div class="hero-scroll-indicator">
    <div class="scroll-arrow"></div>
  </div>
</section>

<!-- Stats Section -->
<section class="stats-section">
  <div class="stats-grid">
    <div class="stat-item">
      <span class="stat-number" id="activePlayers">0</span>
      <span class="stat-label">Active Players</span>
    </div>
    <div class="stat-item">
      <span class="stat-number" id="totalServers">0</span>
      <span class="stat-label">Online Servers</span>
    </div>

    <div class="stat-item">
      <span class="stat-number" id="totalMods">50+</span>
      <span class="stat-label">Exclusive Mods</span>
    </div>
  </div>
</section>

<!-- Features Section -->
<section class="features">
  <div class="feature-card">
    <i class="fas fa-rocket feature-icon"></i>
    <h3>Optimized Performance</h3>
    <p>Our servers are optimized to provide the best gaming experience with minimal latency and maximum stability.</p>
    <a href="/ips" class="btn btn-sm">View Servers</a>
  </div>
  
  <div class="feature-card">
    <i class="fas fa-puzzle-piece feature-icon"></i>
    <h3>Exclusive Mods</h3>
    <p>Enjoy unique mods and exclusive content that you won't find on any other ARK server.</p>
    <a href="/cave-mods" class="btn btn-sm">View Mods</a>
  </div>
  
  <div class="feature-card">
    <i class="fas fa-users feature-icon"></i>
    <h3>Active Community</h3>
    <p>Join a vibrant community of passionate players. Events, tournaments and constant activities.</p>
    <a href="/leaderboard" class="btn btn-sm">View Ranking</a>
  </div>
  
  <div class="feature-card">
    <i class="fas fa-shield-alt feature-icon"></i>
    <h3>Advanced Anti-Cheat</h3>
    <p>Play with peace of mind knowing that our systems protect the integrity of the game.</p>
    <a href="/settings" class="btn btn-sm">More Info</a>
  </div>
  
  <div class="feature-card">
    <i class="fas fa-calendar-alt feature-icon"></i>
    <h3>Special Events</h3>
    <p>Participate in unique events, tournaments and special activities with exclusive rewards.</p>
    <a href="/store" class="btn btn-sm">View Events</a>
  </div>
  
  <div class="feature-card">
    <i class="fas fa-headset feature-icon"></i>
    <h3>24/7 Support</h3>
    <p>Our support team is available 24 hours a day to help you with any issues.</p>
    <a href="/settings" class="btn btn-sm">Contact</a>
  </div>
</section>

<!-- Call to Action Section -->
<section class="cta-section">
  <h2>Ready for Adventure?</h2>
  <p>Join thousands of players who are already enjoying the best ARK: Survival Evolved experience on our servers.</p>
  <div class="cta-buttons">
    <a href="/ips" class="btn btn-primary">
      <i class="fas fa-play"></i>
      Connect Now
    </a>
    <a href="/store" class="btn btn-accent">
      <i class="fas fa-gift"></i>
      View Offers
    </a>
  </div>
</section>

<!-- News Section -->
<section class="news-section">
  <h2>Latest News</h2>
  <div class="news-grid">
    <div class="news-card">
      <div class="news-image"></div>
      <div class="news-content">
        <div class="news-date">December 15, 2024</div>
        <h3 class="news-title">New Fjordur Server</h3>
        <p class="news-excerpt">We've added a new Fjordur server with exclusive mods and optimized configuration for the best gaming experience.</p>
        <a href="#" class="btn btn-sm">Read More</a>
      </div>
    </div>
    
    <div class="news-card">
      <div class="news-image"></div>
      <div class="news-content">
        <div class="news-date">December 10, 2024</div>
        <h3 class="news-title">Special Christmas Event</h3>
        <p class="news-excerpt">Celebrate Christmas with our special event! Unique rewards, festive creatures and much more.</p>
        <a href="#" class="btn btn-sm">Read More</a>
      </div>
    </div>
    
    <div class="news-card">
      <div class="news-image"></div>
      <div class="news-content">
        <div class="news-date">December 5, 2024</div>
        <h3 class="news-title">Mods Update</h3>
        <p class="news-excerpt">We've updated our exclusive mods with new creatures, structures and exciting content.</p>
        <a href="#" class="btn btn-sm">Read More</a>
      </div>
    </div>
  </div>
</section>

<!-- Quick Links Section -->
<section class="quick-links">
  <div class="card glass-effect">
    <div class="card-header">
      <h3><i class="fas fa-link"></i> Quick Links</h3>
    </div>
    <div class="card-body">
      <div class="d-flex flex-wrap gap-2">
        <a href="/ips" class="btn btn-sm">Servers</a>
        <a href="/store" class="btn btn-sm">Store</a>
        <a href="/leaderboard" class="btn btn-sm">Ranking</a>
        <a href="/cave-mods" class="btn btn-sm">Mods</a>
        <a href="/settings" class="btn btn-sm">Settings</a>
      </div>
    </div>
  </div>
</section>

<script>
// Función para cargar estadísticas de servidores
async function loadServerStats() {
    try {
        const response = await fetch('/api/servers/stats');
        const data = await response.json();
        
        if (data.success) {
            updateStats(data.data);
        }
    } catch (error) {
        console.error('Error loading server stats:', error);
    }
}

// Función para actualizar las estadísticas en la página
function updateStats(stats) {
    // Actualizar jugadores activos
    const activePlayersElement = document.getElementById('activePlayers');
    if (activePlayersElement) {
        animateNumber(activePlayersElement, 0, stats.totalPlayers, 1000);
    }
    
    // Actualizar servidores online
    const totalServersElement = document.getElementById('totalServers');
    if (totalServersElement) {
        animateNumber(totalServersElement, 0, stats.onlineServers, 1000);
    }
    

    
    // Actualizar estadísticas del hero
    const heroActivePlayersElement = document.getElementById('heroActivePlayers');
    if (heroActivePlayersElement) {
        animateNumber(heroActivePlayersElement, 0, stats.totalPlayers, 1000);
    }
    
    const heroTotalServersElement = document.getElementById('heroTotalServers');
    if (heroTotalServersElement) {
        animateNumber(heroTotalServersElement, 0, stats.onlineServers, 1000);
    }
}

// Función para animar números
function animateNumber(element, start, end, duration) {
    const startTime = performance.now();
    const startValue = start;
    const endValue = end;
    
    function updateNumber(currentTime) {
        const elapsed = currentTime - startTime;
        const progress = Math.min(elapsed / duration, 1);
        
        const currentValue = Math.floor(startValue + (endValue - startValue) * progress);
        element.textContent = currentValue;
        
        if (progress < 1) {
            requestAnimationFrame(updateNumber);
        }
    }
    
    requestAnimationFrame(updateNumber);
}

// Función para actualizar estadísticas automáticamente
function startStatsAutoRefresh() {
    // Actualizar cada 30 segundos
    setInterval(loadServerStats, 30000);
}

// Cargar estadísticas al cargar la página
document.addEventListener('DOMContentLoaded', () => {
    loadServerStats();
    startStatsAutoRefresh();
});
</script>